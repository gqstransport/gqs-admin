# gqs-backend (PORT=4000). Ensure gqs-backend CORS_ORIGINS includes http://localhost:5173
# Admin login uses gqs-backend ADMIN_EMAIL / ADMIN_PASSWORD (not set here)
VITE_API_URL=http://localhost:4000
